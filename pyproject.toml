[project]
name = "manim-tests"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "manim-slides>=5.5.4",
]

[tool.uv.workspace]
members = [
    "manimations",
]
